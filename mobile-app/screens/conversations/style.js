import { StyleSheet } from 'react-native';
import myColors from '../../constants/colors';

const styles = StyleSheet.create({
    container: {
        flex: 1,
    },
    conservationsWrapper: {
        flex: 1,
        backgroundColor: myColors.sixth,
    },
});

export default styles;
